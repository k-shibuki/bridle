% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constraints.R
\name{Constraint}
\alias{Constraint}
\title{Constraint}
\usage{
Constraint(
  id = character(0),
  source = character(0),
  type = character(0),
  param = character(0),
  condition = character(0),
  forces = list(),
  requires = list(),
  values = character(0),
  incompatible = list(),
  enabled_when = character(0),
  message = character(0),
  confirmed_by = character(0),
  confidence = character(0)
)
}
\arguments{
\item{id}{Unique identifier (character).}

\item{source}{Extraction source (character). One of \code{"formals_default"},
\code{"rd_description"}, \code{"source_code"}, \code{"expert"}.}

\item{type}{Constraint type (character). One of \code{"forces"}, \code{"requires"},
\code{"valid_values"}, \code{"incompatible"}, \code{"conditional"}.}

\item{param}{Target parameter name (character).}

\item{condition}{R expression for when this constraint fires (character).}

\item{forces}{Forced parameter values (named list).}

\item{requires}{Required parameter values (named list).}

\item{values}{Valid value list (character vector).}

\item{incompatible}{Incompatible value combinations (named list).}

\item{enabled_when}{Condition under which the parameter is effective
(character).}

\item{message}{Human-readable message (character).}

\item{confirmed_by}{Layers that confirmed this constraint (character vector).}

\item{confidence}{Confidence grade (character). One of \code{"high"}, \code{"medium"},
\code{"low"}, \code{"gap"}.}
}
\description{
A single inter-parameter technical constraint. The \code{type}
field determines which optional fields are semantically required.
}
