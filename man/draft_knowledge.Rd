% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draft_knowledge.R
\name{draft_knowledge}
\alias{draft_knowledge}
\title{Draft Knowledge Plugin Artifacts}
\usage{
draft_knowledge(
  scan_result,
  references = NULL,
  provider = NULL,
  model = NULL,
  output_dir = "."
)
}
\arguments{
\item{scan_result}{A \link{ScanResult} or \link{PackageScanResult} object.}

\item{references}{Optional list of reference metadata from
\code{\link[=fetch_references]{fetch_references()}}.}

\item{provider}{LLM provider name for ellmer (character), e.g.
\code{"anthropic"}, \code{"openai"}. If \code{NULL}, uses ellmer default.}

\item{model}{Model name (character). If \code{NULL}, uses provider default.}

\item{output_dir}{Directory to write draft YAML files (character).}
}
\value{
A list with \code{decision_graph}, \code{knowledge}, \code{constraints},
\code{context_schema}, and \code{manifest} elements.
}
\description{
Uses ellmer to generate draft plugin artifacts (decision graph,
knowledge entries, technical constraints) from a \link{ScanResult} and
fetched references.

Assembles a prompt from scan data and optional references,
sends it to an LLM via ellmer, and writes draft YAML files to
the specified output directory. Accepts either a \link{ScanResult}
(single function) or a \link{PackageScanResult} (full package).
}
\details{
In addition to the three LLM-generated sections (decision graph,
knowledge, constraints), this function generates \code{context_schema.yaml}
and \code{manifest.yaml} heuristically from the decision graph structure.
}
