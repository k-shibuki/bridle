% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridle_agent.R
\name{bridle_agent}
\alias{bridle_agent}
\title{Bridle Agent}
\usage{
bridle_agent(
  plugin_dir,
  provider = NULL,
  model = NULL,
  log_dir = NULL,
  sandbox_timeout = 10
)
}
\arguments{
\item{plugin_dir}{Path to plugin directory containing YAML files
(\code{decision_graph.yaml}, \code{knowledge.yaml}, \code{constraints.yaml},
\code{context_schema.yaml}).}

\item{provider}{LLM provider name (character or NULL for auto-detect).
See \code{\link[=bridle_runtime_chat]{bridle_runtime_chat()}} for resolution order.}

\item{model}{LLM model name (character or NULL for provider default).}

\item{log_dir}{Directory for JSONL decision logs (character or NULL
to disable logging).}

\item{sandbox_timeout}{Timeout in seconds for code execution (numeric).}
}
\value{
A list with class \code{"bridle_agent"} containing graph, knowledge,
constraints, logger, sandbox, provider, model, and a \code{console()} method.
The graph engine is accessed with \verb{$engine} (stored in an internal
\code{.runtime} environment so mutations from helper functions update the same
runtime instance).
}
\description{
Creates a runtime agent that orchestrates all Phase 2 components:
graph traversal, knowledge retrieval, prompt assembly, LLM chat,
response parsing, code execution, and decision logging.

Initializes all runtime components from a plugin directory
containing YAML configuration files.
}
