% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decision_graph.R
\name{Transition}
\alias{Transition}
\title{Transition}
\usage{
Transition(
  to = character(0),
  when = character(0),
  computable_hint = character(0),
  always = NA,
  otherwise = NA
)
}
\arguments{
\item{to}{Target node ID (character).}

\item{when}{Natural-language condition text (character). Mutually exclusive
with \code{always} and \code{otherwise}.}

\item{computable_hint}{R expression hint for \code{when} (character). Only valid
when \code{when} is also specified.}

\item{always}{If \code{TRUE}, transition unconditionally (logical).}

\item{otherwise}{If \code{TRUE}, transition as fallback (logical).}
}
\description{
A directed edge between two nodes in the decision graph.
Exactly one condition field must be specified: \code{when}, \code{always}, or
\code{otherwise}. \code{computable_hint} is only valid alongside \code{when}.
}
