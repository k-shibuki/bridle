---
description: "Creating, reviewing, or modifying tests: test perspectives table, coverage, and mock conventions"
alwaysApply: false
---
# Test Conventions

## Prerequisite

Per `workflow-policy.mdc` § Knowledge Consultation Triggers, read the listed atoms before starting work.

---

## 1. Test Code Implementation Policy

1. Implement **all** cases listed in the test perspectives table as automated tests.
2. Always include **at least as many failure cases** (validation errors, exceptions, external dependency failures) as success cases.
3. Cover the following perspectives in tests:
   - Normal cases (main scenarios)
   - Abnormal cases (validation errors, exception paths)
   - Boundary values (0, min, max, ±1, empty, NULL)
   - Invalid type/format inputs
   - External dependency failures (API, ellmer/ragnar calls, etc., where applicable)
   - Exception types and error messages
4. **When adding new parameters/fields, don't complete with just "value verification."** Include at least one (preferably both) of the following so tests fail when added parameters are "not propagated/used anywhere":
   - **Propagation (wiring) verification**: Assert that downstream function arguments include the added parameter (mock call argument verification, generated request verification, etc.)
   - **Behavior (effect) verification**: Assert that changing the added parameter value changes output/side effects as specified
5. Target 100% branch coverage, designing additional cases as needed.
   100% branch coverage is a "target value"; when not reasonably achievable, cover at least high-business-impact branches and main error paths.
   When branches remain uncovered, state the reason and impact in `Notes` or PR body.
6. **S7 constructor testing**: Test validator error messages (match stable substring), property type constraints (`expect_error` on wrong type), and default values explicitly.
7. **Internal function testing**: Use `bridle:::function_name` for non-exported functions with non-trivial logic. Place in `test-internals.R`.

---

## 2. Given / When / Then Comments

Every test case MUST use the Given/When/Then comment format. See `@.cursor/knowledge/test--given-when-then.md` for the template and example.

---

## 3. Exception/Error Verification

1. For cases where exceptions occur, explicitly verify exception **type** and **message** (e.g., `expect_error(..., class = "error")`).
2. For validation-type abnormal cases, also verify error codes and field information if available.
3. When simulating external dependency failures, use mocks to verify expected exceptions/retries/fallbacks are called.

---

## 4. Operational Notes

1. Reject test diffs that don't comply with this rule in review.
2. Even without external dependencies, **use mocks to simulate failures** for failure cases.
3. PRs with meaningful changes to production code (spec additions, bug fixes, refactors affecting behavior) must include corresponding automated test additions or updates.

---

## 5. Mock/Patch Conventions (R / testthat)

1. **MUST apply mocks at the use site** with `testthat::local_mocked_bindings()`.
   - Mock the function as it is used in the code under test (the binding in the package namespace).
2. **MUST NOT weaken tests to make them pass.**
   - Removing/relaxing assertions to make tests pass conceals bugs and violates wiring/effect principles.
   - Investigate root cause (incorrect mock target, missing setup, etc.) before fixing.
3. **MUST use `withr::local_envvar()` and `withr::local_options()`** for temporary environment/options changes; they auto-restore after the test.
4. **MUST create thin wrappers** for external package functions (ellmer, ragnar) and mock those wrappers, since namespace locking can prevent mocking base/external functions directly.

For R-specific testing gotchas (mock scoping constraints, NULL assignment trap, layered mock strategy), read the atoms listed in § Prerequisite above.

---

## 6. References

- **Test helpers**: `@.cursor/knowledge/test--helper-conventions.md` (helper files, setup, naming)
- **Snapshot testing**: `@.cursor/knowledge/test--snapshot-guidelines.md` (`expect_snapshot`, `_snaps/`)
- **Coverage policy**: `@.cursor/commands/verify.md` § Act (coverage-verify step); `@.cursor/knowledge/ci--job-dependency-graph.md` (enforcement model, thresholds). Coverage is not on the PR CI critical path.

---

Follow this rule, always self-check for missing perspectives when designing and implementing tests.
