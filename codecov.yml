# Coverage thresholds — SSOT: .cursor/rules/test-strategy.mdc § Coverage Threshold Policy
coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 2%
    patch:
      default:
        target: 90%

ignore:
  - "tools/"
  - "containers/"
  - "docs/"
  - "tests/"
